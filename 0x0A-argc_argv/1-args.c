#include <stdio.h>

/**
 * main - assigning arguments to main
 *
 * @argc: size of argv
 * @argv: array of size argc
 *
 * Return: Always 0.
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
